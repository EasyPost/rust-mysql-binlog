This is a clean implementation of a MySQL binlog parser in Rust, including support for the JSONB type.

Its primary purpose is handling row-based logging.

It's been tested against MySQL 5.6 and 5.7.
